package tests

import (
	"testing"
)

func TestDatatypes_Print(t *testing.T) {
	//typeMap := make(map[string][]string)
	//for _, v := range types.Typ {
	//	typeMap[v.String()] = make([]string, 0)
	//}
	//for _, v := range types.Typ {
	//	for _, t := range types.Typ {
	//		if types.ConvertibleTo(v, t) && !strings.Contains(v.String(), "type") && !strings.Contains(t.String(), "type") {
	//			typeMap[v.String()] = append(typeMap[v.String()], t.String())
	//		}
	//	}
	//}
	//for k, v := range typeMap {
	//	if !strings.Contains(k, "type") {
	//		fmt.Println(k, v)
	//	}
	//}
}
